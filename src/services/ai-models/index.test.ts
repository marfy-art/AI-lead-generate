import {beforeEach,describe,expect,it} from "vitest";
import {activateAiConfig,addAiConfig,clearAiConfigsForTests,listAiConfigs,removeAiConfig,resolveActiveAiConfig} from ".";
beforeEach(clearAiConfigsForTests);
describe("AI model configuration",()=>{it("never returns the API key or ciphertext in public configuration",()=>{const item=addAiConfig({workspaceId:"w",name:"Primary",provider:"openai",baseUrl:"https://api.openai.com/v1",model:"gpt-test",apiKey:"secret-key-1234",active:true});expect(item).not.toHaveProperty("apiKey");expect(item).not.toHaveProperty("apiKeyEncrypted");expect(item.maskedKey).toBe("••••••••1234");expect(resolveActiveAiConfig("w")?.apiKey).toBe("secret-key-1234");});it("keeps one active model per workspace",()=>{const first=addAiConfig({workspaceId:"w",name:"One",provider:"openai",baseUrl:"https://api.openai.com/v1",model:"one",apiKey:"key-one",active:true});const second=addAiConfig({workspaceId:"w",name:"Two",provider:"openai_compatible",baseUrl:"https://models.example.com/v1",model:"two",apiKey:"key-two"});activateAiConfig("w",second.id);expect(listAiConfigs("w").find(item=>item.id===first.id)?.active).toBe(false);expect(removeAiConfig("w",second.id)).toBe(true);});});
