import {NextResponse} from "next/server";
import {leads} from "@/lib/mock-data";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const lead=leads.find(item=>item.id===id);if(!lead)return NextResponse.json({error:"Lead not found"},{status:404});const fields=[{field:"company",value:lead.company,sourceProvider:"discovery",verificationStatus:"observed",confidence:lead.confidence/100,isInferred:false},{field:"person",value:lead.person,sourceProvider:"mock_apollo",verificationStatus:"provider_data",confidence:lead.confidence/100,isInferred:false},{field:"work_email",value:lead.email??"Not found",sourceProvider:lead.email?"mock_hunter":"none",verificationStatus:lead.emailStatus.toLowerCase().replaceAll(" ","_"),confidence:lead.emailStatus==="Verified"?.96:lead.emailStatus==="Unverified"?.55:0,isInferred:false},{field:"phone",value:lead.phone??"Not found",sourceProvider:lead.phone?"mock_provider":"none",verificationStatus:lead.phoneType.toLowerCase().replaceAll(" ","_"),confidence:lead.phoneType==="Direct work"?.9:lead.phoneType==="Company main"?.65:0,isInferred:false}];return NextResponse.json({leadId:id,fields,evidence:lead.evidence.map(item=>({...item,isInferred:item.kind==="AI inference"})),mode:"mock"});}
